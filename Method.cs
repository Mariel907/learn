using System;

/*
namespace Project_Program_104
{
    internal class Method
    {
        public static void Main(string[] args)
        {
            int a,b,c,d;
            int addresult = 0;

            a = 5;
            b = 3;
            performAddOperation(a, b);

            c = 15;
            d = 10;
            performAddOperation(c, d);

            Console.ReadKey();
        }

        static void performAddOperation(int x, int y)
        {
            int addResult = 0;
            addResult = x + y;
            Console.WriteLine($"{x} + {y} = {addResult}");
            DisplayMessage();
        }
        static void DisplayMessage()
        {
            Console.WriteLine("Process is done \nThis process is run by Mariel \nFinished on time:  " +DateTime.Now.ToShortTimeString());
           
        }

        
    }
}
*/




                                                        

                                                                    